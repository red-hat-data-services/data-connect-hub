[files]
extend-exclude = [
    "dc-controller/config/crd/",
    "dc-controller/charts/crds/",
    "docs/api/*.html",
]

[default.extend-words]
# Allow "ba" in hex trace IDs (W3C Trace Context standard uses example ID with "ba")
ba = "ba"
