type: object
description: >
  A connection resource wrapping metadata, spec, and live status.
required: [metadata, resource, status]
properties:
  metadata:
    $ref: "ResourceMetadata.yaml"
  resource:
    $ref: "DataConnection.yaml"
  status:
    $ref: "DataConnectionStatus.yaml"
