type: object
description: Reference to an existing Kubernetes secret containing credentials.
required: [secret]
properties:
  secret:
    type: string
    description: >
      Name of the Kubernetes secret in the tenant namespace. This is a bare
      secret name, not a `namespace/name` pair; cross-namespace references
      are not supported.
    example: my-db-creds
additionalProperties: false
