description: Internal server error
content:
  application/json:
    schema:
      $ref: "../schemas/ErrorResponse.yaml"
    example:
      code: status_update_failed
      message: Failed to update data connection status
